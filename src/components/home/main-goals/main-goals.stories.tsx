import type { Meta } from '@storybook/react';
import { MainGoals } from './main-goals';

const Story: Meta<typeof MainGoals> = {
  component: MainGoals,
  title: 'Components/Home/MainGoals',
};
export default Story;

export const Primary = {
  args: {},
};
